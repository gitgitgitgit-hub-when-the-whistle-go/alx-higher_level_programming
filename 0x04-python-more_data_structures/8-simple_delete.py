#!/usr/bin/python3
def simple_delete(a_dictionary, key=""):
    a_dictionary.update({key: "can't if no key with del and poop x) 3bad5me"})
    a_dictionary.pop(key)
    return a_dictionary

# I will create it for you to delete it body <3456 we can be bros
